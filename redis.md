redis

### Redis有哪些数据类型？

| 数据类型           | 核心特性                                         | 底层实现（分场景）                                                                 | 适用条件（默认配置）                          | 核心操作时间复杂度         | 典型使用场景                         | 关键优化点                                                         |
|--------------------|--------------------------------------------------|------------------------------------------------------------------------------------|-----------------------------------------------|----------------------------|--------------------------------------|--------------------------------------------------------------------|
| 字符串（String）   | 二进制安全，支持数值操作、字符串拼接，键值均为字符串 | 1. 简单动态字符串（SDS）<br>2. 整数编码（存储整数时）<br>3. embstr（短字符串 < 44 字节）<br>4. raw（长字符串≥44 字节） | 无（自动适配）                                | 所有操作 O(1)               | 缓存用户信息、计数器、分布式锁、限流 | 1. 避免存储超大字符串（建议 < 10KB）<br>2. 整数场景用 INCR/DECR 而非 GET+SET |
| 哈希（Hash）       | 结构化存储，支持单字段/批量操作，键值对集合         | 1. 压缩列表（ziplist）：小数据量<br>2. 字典（dict，拉链法+渐进式rehash）：大数据量 | 1. 字段数 < 512<br>2. 所有字段/值长度 < 64 字节 | 单字段操作 O(1)<br>遍历 O(N) | 存储用户/商品结构化信息、缓存数据库查询结果 | 1. 控制字段数（建议 < 1000）<br>2. 小字段优先使用 ziplist 编码                |
| 列表（List）       | 有序可重复，支持两端插入/删除、范围查询             | 1. 压缩列表（ziplist）：小数据量<br>2. 快速链表（quicklist）：大数据量（多个 ziplist 通过双向链表连接） | 1. 元素数 < 512<br>2. 元素长度 < 64 字节        | 两端操作 O(1)<br>中间操作 O(N) | 消息队列、最新评论列表、用户历史记录     | 1. 避免 LRANGE 全量遍历<br>2. 消息队列用 BRPOP 阻塞消费                      |
| 集合（Set）        | 无序不可重复，支持交集/并集/差集                    | 1. 整数集合（intset）：全整数元素<br>2. 字典（dict）：非整数/大数据量             | 1. 元素全为整数<br>2. 元素数 < 512              | 增删查 O(1)<br>集合运算 O(N)  | 标签系统、共同好友、去重计数           | 集合运算元素数不宜过大（建议 < 10000），避免阻塞主线程                       |
| 有序集合（Zset）   | 有序不可重复，按 score 排序，支持范围/排名查询       | 1. 压缩列表（ziplist）：小数据量<br>2. 跳表（skiplist）+字典（dict）：大数据量（跳表排序，字典查score） | 1. 元素数 < 128<br>2. 元素长度 < 64 字节        | 增删查 O(logN)<br>范围查询 O(N) | 排行榜、延时队列、积分排序             | 控制元素数，避免 ZRANGE 全量查询                                         |
| 位图（Bitmap）     | 扩展类型，按位操作，极致节省内存                    | 基于 String（SDS），按位存储 0/1 状态                                              | 无（复用 String 存储）                         | 按位操作 O(1)                | 用户签到、在线状态、布隆过滤器         | 1. 合理规划位偏移量<br>2. 单 key 大小建议 < 1GB                              |
| 基数统计（HyperLogLog） | 扩展类型，极小内存统计海量数据基数（误差≈0.81%）      | 基于 String（SDS），存储概率算法核心数据                                            | 无（复用 String 存储）                         | 添加元素 O(1)<br>统计基数 O(1) | 网站 UV 统计、用户访问量计数           | 仅用于基数统计，不支持获取具体元素                                         |
| 地理信息（Geo）    | 扩展类型，支持经纬度存储、距离计算、范围查询         | 基于 Zset（跳表+字典），经纬度编码为 score                                         | 无（复用 Zset 存储）                           | 增删查 O(logN)<br>距离计算 O(1) | 附近的人、门店距离排序                 | 避免存储海量地理点位，影响排序性能                                         |

### redis为什么这么快？
* 纯内存操作，彻底规避磁盘 I/O 瓶颈
* 单线程执行模型，避免多线程的核心开销（线程上下文切换开销，锁竞争 / 死锁问题；单线程不一定代表慢）
* 高效的网络 I/O 模型：I/O 多路复用（select/poll/epoll，水平触发/边沿触发 redis采用边沿触发，为什么？ 红黑树/平衡树）
* Redis 6.0 多线程优化：仅将「网络数据的读取（read）和写入（write）」交给多线程处理，命令解析、执行、内存操作仍由单线程完成，既利用多核提升网络吞吐，又保留单线程的无锁优势。

### RDB持久化方式
* 触发方式：手动/自动/隐式（主从同步/执行 FLUSHALL/redis正常关闭）
* 核心优势
    * 高性能：持久化由子进程完成，主线程无阻塞，对 Redis 读写性能几乎无影响；
    * 文件体积小：二进制压缩存储，相同数据量下，RDB 文件体积远小于 AOF 文件，便于备份、传输（如主从同步）；
    * 恢复速度快：二进制文件直接加载到内存，无需解析执行命令，数据量越大，恢复效率越优于 AOF；
    * 适合大规模数据备份：全量快照特性，适合做定时冷备（如每天凌晨备份一次 RDB 文件，存储到云对象存储）。
* 核心缺陷
    * 数据丢失风险：RDB 是定时全量快照，若 Redis 宕机（如服务器掉电），会丢失最后一次 RDB 到宕机前的所有数据，丢失量由配置的触发策略决定（如 5 分钟触发一次，最多丢失 5 分钟数据）；
    * 频繁触发会增加服务器开销：每次 BGSAVE 都会 fork() 子进程，若频繁触发（如 1 分钟一次），fork() 的内存页表拷贝、子进程的内存遍历会增加 CPU / 内存开销，影响 Redis 性能；
    * 不适合实时持久化：全量快照的特性决定了 RDB 无法实现「每一条写命令都落地」，无法满足金融、支付等零数据丢失的业务场景。

### AOF持久化方式
* 命令追加：所有写命令（读命令不记录）会被格式化为「RESP 协议」的文本格式，追加到内存中的 AOF 缓冲区（避免直接写磁盘导致的频繁 I/O 阻塞）；
* 刷盘落盘：缓冲区数据按配置的策略刷写到磁盘的 AOF 文件，保证命令落地；（Always，everysec，no）
* 文件重写：AOF 文件会随写命令累积越来越大，通过「AOF 重写」生成精简的新文件（合并重复命令、删除无效命令），替换旧文件。

### redis如何恢复数据
优先使用AOF

### redia主从复制
* 作用：读写分离；数据备份；高可用
* 时机：从节点首次连接主节点、主节点运行 ID（runid）变化、从节点偏移量（offset）超出主节点复制缓冲区范围；
* 同步流程（全量同步+增量同步）
    * 从节点通过执行 replicaof 命令连接到主节点,从节点向主节点发送 `PSYNC ? -1` 命令，请求数据同步;
    * 主节点发送FULLRESYNC <runid> <offset>，响应全量同步
    * 主节点 fork() 子进程生成 RDB 文件（利用 COW 机制，不阻塞主线程）；
    * 主节点将 RDB 文件发送给从节点，从节点清空本地数据后加载 RDB；
    * 主节点将生成 RDB 期间的写命令存入「复制缓冲区」，同步完成后发送给从节点，从节点执行命令追平数据。
    * 主从完成全量同步后，主要依靠传播命令阶段来保持数据的增量同步。主节点会将每次执行的写命令实时发送给所有从节点。
    * 增量复制时，主节点会把要同步的写命令暂存一份到复制积压缓冲区。这样当从节点和主节点发生网络断连，从节点重新连接后，可以从复制积压缓冲区中复制尚未同步的写命令。


### 主从复制有哪些问题以及解决方案
    * 从节点同步慢 / 数据不一致
        * 原因
            * 主从网络延迟高；
            * 复制缓冲区过小，断连后触发全量同步；
            * 主节点写压力过大，无法及时同步命令。
        * 解决方案
            * 增大复制缓冲区：repl-backlog-size 50mb；
            * 优化网络：将主从节点部署在同一机房，使用高速网络；
            * 读写分离：减少主节点读压力，专注写操作；
            * 级联复制：搭建「主→从→从」架构，减轻主节点同步压力。
    * 主节点故障后数据丢失
        * 原因
            * 主节点异步同步，故障时部分命令未同步到从节点；
            * 主节点未开启持久化，重启后数据清空。
        * 解决方案
            * 开启主节点持久化（RDB+AOF 混合模式），避免重启后数据丢失；
            * 配置 min-replicas-to-write 1 和 min-replicas-max-lag 5，主节点至少有 1 个从节点（延迟 < 5s）才允许写，减少数据丢失；
            * 结合哨兵（Sentinel）实现自动故障转移，缩短故障时间。
    * 从节点加载 RDB 阻塞
        * 原因
            * 从节点加载 RDB 文件时会阻塞主线程，无法处理读请求；
            * RDB 文件过大，加载时间过长。
        * 解决方案
            * 选择业务低峰期进行首次同步；
            * 使用无盘同步（repl-diskless-sync yes），避免从节点加载大 RDB 文件；
            * 拆分主节点数据，减少单节点数据量。
    * 主从切换后数据不一致
        * 原因
            * 旧主节点恢复后，与新主节点数据冲突；
            * 故障转移时，从节点未追平主节点数据。
        * 解决方案
            * 故障恢复后让主节点变成从节点
            * 哨兵模式下down-after-milliseconds调大一点，等待从节点数据追平

### 哨兵模式
* 功能：定时监控、主观下线、领导者选举和故障转移。

### cluster模式
* Redis 集群的搭建始于节点的添加和握手。每个节点通过设置 cluster-enabled yes 来开启集群模式。然后通过 CLUSTER MEET 进行握手。由于采用了 Gossip 协议，我们不需要让每对节点都执行握手
* 握手完成后，可以通过 CLUSTER ADDSLOTS 命令为主节点分配哈希槽。
* 需要扩容时，首先通过 CLUSTER MEET 命令将新节点加入集群；然后使用 reshard 命令将部分哈希槽重新分配给新节点。
* 缩容则是反向操作：先将要下线节点负责的所有槽迁移到其他节点，再通过 CLUSTER FORGET 命令将节点从集群中移除。整个伸缩过程支持在线操作，无需停机，得益于 Redis 集群的 MOVED 和 ASK 重定向机制。当客户端访问的键不在当前节点时，会收到重定向响应，指引它连接到正确的节点。


### 缓存击穿核心解决方案（6 种，生产优先前 3 种）
* 所有方案的核心目标分为两类：
    * 预防类：从根源避免热点 Key 的缓存失效，让请求始终走缓存（推荐首选）；
    * 兜底类：若缓存仍失效，通过加锁 / 限流等方式，保证只有一个请求打库更新缓存，其余请求等待 / 走兜底逻辑。
* 方案 1：热点 Key 永不过期
    * 业务判断或者通过监控工具（如 Prometheus、Redis-cliINFO stats）识别热点 Key，手动设置永不过期
    * 优势：实现零成本、性能最优（无失效逻辑，无额外开销）、彻底解决击穿；
    * 劣势：需精准识别热点 Key（漏判则失效）、缓存占用永久内存（需控制 Key 数量，避免内存溢出）。
    * 补充：热点 Key 更新策略：先更数据库，再更缓存；
* 方案 2：互斥锁（分布式锁）—— 失效时单请求打库（通用兜底方案）
    * 利用分布式锁的互斥性，保证同一时间只有一个请求打库，避免并发穿透，是缓存击穿的通用兜底方案（分布式锁实现方式）
    * 注意事项：锁过期时间/锁粒度/释放锁/重试机制
    * 优势：通用型强、无需识别热点 Key、能有效控制打库请求数；
    * 劣势：有锁开销（Redis 网络请求）、未获取锁的请求会短暂等待（性能略降）、存在锁竞争风险。
* 方案 3：逻辑过期（无锁方案，高并发优选）
    * 对热点 Key 的缓存值增加「逻辑过期时间字段」，而非使用 Redis 的原生过期时间。求读取缓存时，先判断逻辑过期时间是否已到，未过期：直接返回缓存值；已过期：不立即打库，而是返回旧缓存值，同时异步开启一个线程去更新数据库和缓存的逻辑过期时间。牺牲少量数据一致性，换取无锁 + 无请求等待，保证高并发下请求始终能拿到数据（旧值），同时异步更新缓存
    * 优势：无锁无等待（性能最优）、高并发友好、避免打库；
    * 劣势：牺牲数据一致性（短时间返回旧值）、需要封装缓存值、异步线程需管理（避免线程池溢出）。
* 方案 4：预热缓存（提前加载，预防失效）
* 方案 5：限流 & 熔断（终极兜底，防止数据库宕机）
* 方案 6：增加缓存过期时间随机值（避免集中失效，辅助方案）

### 缓存穿透
* 方案 1：空值缓存（生产通用基础方案，必做）
* 方案 2：请求层参数校验（前置拦截，源头过滤）
* 方案 3：布隆过滤器（高并发 / 海量无效 Key 场景首选）
* 方案 4：布隆过滤器升级 —— 计数布隆过滤器（支持 Key 删除）

### 缓存雪崩（大量key失效）
* 原因可能是同时到期也有可能是redis集群挂掉了，所以核心方案是按防护层级排序
* 第一层：预防层 - 避免大量 Key 集中失效（从根源减少雪崩概率）
    * 给 Key 的过期时间添加随机值（生产必做，基础优化）
    * 热点 Key 永不过期（核心热点兜底）
    * 分层缓存设计（多级缓存，分散 Redis 压力）
* 第二层：容错层 - 提升缓存层高可用（防止 Redis 服务宕机）
    * Redis 集群化部署（主从 + 哨兵 / Redis Cluster）
    * 缓存服务熔断 & 限流（防止 Redis 故障扩散）
* 第三层：兜底层 - 保护数据库（雪崩发生后的终极防护）
    * 数据库层限流 & 熔断（终极兜底）
    * 服务降级 / 熔断（业务层兜底）

### 缓存与数据库一致性问题
* 有缓存与数据库一致性方案的设计，都需遵循以下 3 个核心原则，避免从根源上出现一致性问题：
    * 原则 1：缓存仅作为数据库的「只读副本」：缓存中的数据只能从数据库加载 / 更新，禁止直接修改缓存
    * 原则 2：根据业务场景选择一致性级别
        * 强一致：金融交易、支付、库存扣减（数据更新后，缓存和数据库必须实时一致，不允许脏数据）；
        * 最终一致：商品信息、用户资料、文章内容（允许短时间内缓存与数据库不一致，最终通过过期 / 更新达成一致，读多写少场景首选）；
        * 弱一致：推荐列表、热点排行、统计数据（允许一定时间的脏数据，性能优先）。
    * 原则 3：读多写少用缓存，写多读多慎使用
* 缓存方案
    * 方案 1：先更新数据库，再更新缓存（Write Through）
        * 优点：实现简单、缓存始终是最新值，读请求无脏数据；
        * 缺点：
            * 写操作冗余：若数据更新后长期不被读取，更新缓存的操作无意义，浪费 Redis 资源；
            * 并发更新脏数据：多线程并发更新时，会出现时序覆盖（如线程 A 更 DB→线程 B 更 DB→线程 B 更缓存→线程 A 更缓存），导致缓存存旧值；
            * 缓存更新失败：数据库更新成功，缓存更新失败（如网络抖动），导致缓存脏数据。
        * 结论：生产环境极少使用，仅适用于无并发、写极少的静态数据。
    * 方案 2：先删除缓存，再更新数据库（Write Invalidate 反模式）
        * 结论： 并发读写导致的脏数据（最致命） ，生产环境禁止使用
    * 方案 3：先更新数据库，再删除缓存（Cache Aside 缓存旁路，生产首选）
        * 优点：规避了方案一的问题，几乎无脏数据；且没有多余操作
        * 缺点：可能会删除失败，重试可解决。但是极端情况还是会有脏数据（缓存不存在-> 线程A读DB ->  B 更 DB -> B删缓存 -> A 跟新缓存）
        * 结论：生产最常用，适配几乎所有所有读多写少场景。删除失败问题可以通过重试解决。还可以使用延迟双删方案解决
    * 方案 4: 并发更新解决方案：分布式锁 + Cache Aside
        * 适合场景：强一致性，高并发写
    * 终极方案：基于 Binlog 的缓存自动同步

* 总结
    * 缓存与数据库一致性的核心：取舍「性能」和「一致性」，99% 的业务选择最终一致即可，强一致仅适用于金融、支付等核心场景；
    * 生产首选方案：Cache Aside（先更 DB，再删缓存） + 本地重试 + MQ 兜底，简单、稳妥、适配所有读多写少场景；
    * 强一致方案：分布式锁 + Cache Aside，保证同一时间只有一个线程更新数据，从根源避免并发一致性问题；
    * 分布式微服务终极方案：Canal/MaxWell 监听 Binlog + 自动删缓存，无代码侵入，解决跨服务一致性问题；
    * 核心避坑：禁止使用「先删缓存，再更数据库」的反模式，缓存操作容忍失败，数据库是唯一的真实数据来源；
    * 最终兜底：无论采用哪种方案，给缓存设置合理的过期时间是最后的一致性保障，确保脏数据不会长期存在。
        

### 分布式锁

### redis底层结构
sds/ziplist/quicklist/dict/跳表





