# Query 分析（Query Understanding/Query Processing）


Query 分析是搜索系统的 "入口大脑"，核心是把用户输入的自然语言文本，清洗、解析、理解、结构化，转化为系统能精准执行的检索指令，直接决定召回和排序的质量天花板。

## 一、基础层：Query 预处理（清洗与归一）

先做文本标准化，消除噪声：

- **大小写 / 全半角 / 简繁体归一**：统一格式（如 ＡＰＰ → app、蘋果 → 苹果）
- **特殊字符过滤**：去除表情、乱码、无意义符号、多余空格
- **长度截断 / 补齐**：超长 query 截断、过短 query 标记
- **停用词过滤**：去掉 "的、了、吗、在" 等无检索价值虚词

## 二、核心层：分词与语言解析

中文搜索最基础步骤：

### 中文分词

- 把连续字符串切成有意义词：深度学习入门 → 深度/学习/入门
- 混合粒度：同时保留单字、词、短语（如 极客时间 → 极客、时间、极客时间）
- 歧义消解：美国会 → 政治场景：美 / 国会；日常：美国 / 会

### 词性标注（POS）

- 标注名词、动词、形容词、品牌词、时间词等

### 实体识别（NER）

- 提取人名、地名、机构名、商品名、时间、型号等关键实体
- 例：2026款华为Mate70价格 → 时间 (2026 款)、品牌 (华为)、产品 (Mate70)

## 三、质量层：纠错、改写、归一

修正用户输入误差、消除歧义：

### 拼写纠错

- 搜素引擎 → 搜索引擎
- 平果电脑 → 苹果电脑
- 拼音纠错 / 形近字纠错

### Query 归一化

- 同义归一：电褥子 ↔ 电热毯、电脑 ↔ 计算机
- 简称 / 全称归一：北大 ↔ 北京大学
- 格式归一：2026/3/31 → 2026年03月31日

### Query 改写（意图对齐）

- 补全：Python → Python教程
- 简化：哪里买性价比高的笔记本 → 高性价比 笔记本 购买
- 同义扩展：召回时加入同义词扩大匹配范围

## 四、理解层：意图与权重分析

判断 "用户到底想找什么、有多重要"：

### 意图分类（核心）

- **导航型**：找官网 / APP（淘宝官网、微信下载）
- **信息型**：查知识 / 教程 / 新闻（什么是NER、Python入门）
- **交易型**：找商品 / 购买（华为Mate70价格、买运动鞋）
- **问答型**：直接要答案（北京今天天气）

### Term 权重 / 重要性分析

- 核心词（高权重）：品牌、型号、核心名词
- 修饰词（低权重）：形容词、副词
- 例：红色华为Mate70 → 权重：华为 Mate70 > 红色

### 类目 / 垂直域预测

- 判定 query 属于电商、新闻、医疗、本地生活等
- 例：感冒吃什么药 → 医疗；附近火锅店 → 本地生活

### 时效性 / 地域识别

- 时间敏感：今日新闻、2026世界杯
- 地域敏感：深圳天气、北京美食

## 五、安全与优化层

- **敏感 / 违规 query 识别与拦截**
- **零结果 / 低结果预判**：提前触发扩展召回策略
- **向量表示（语义嵌入）**：生成 query 向量，用于语义召回
- **搜索建议 / 联想补全**：输入时给出候选提示

## 六、输出：结构化查询指令

最终输出给检索模块：

- 分词结果 + 权重
- 实体列表 + 类型
- 意图标签 + 类目
- 标准 / 扩展词集合
- 向量 embedding

## 七、业务实践

### Query分析服务架构

#### 1. 核心处理流程

**预处理阶段：**

- Query截断与归一化处理
- 垃圾Query过滤与分词
- 同义词获取与标准菜识别

**深度分析阶段：**

- 意图识别：通过QueryIntentService分析用户搜索意图（POI/商品/分类等）
- 实体识别：QueryNerService采用CRF模型识别菜品、品牌等实体
- 分类体系：QueryClassifyService实现商家/商品分类，依赖Redis缓存+模型兜底

**召回优化阶段：**

- QueryRewriteServiceV2生成改写词、纠错词（命中率70%缓存）
- 基于Term权重分析优化召回策略

#### 2. 关键性能指标

| 模块 | 缓存命中率 | 平均耗时 | 资源消耗 |
|------|----------|---------|---------|
| 深度匹配 | 70% | 11ms/batch | 270张P40卡 |
| Query分析 | 97.5% | <50ms | 4张T4卡 |

### Query改写服务中纠错词、改写词和同义词的应用场景区别

#### 1. 纠错词（Correct Words）

**应用场景**：主要用于修正用户输入中的拼写错误或常见输入错误

**数据来源**：存储在waimai_d_search_query_correct表和queryOfflineCorrectDBDict词表中

**典型示例**：将"肯德鸡"纠正为"肯德基"

**处理方式**：直接替换原query中的错误词项

#### 2. 改写词（Rewrite Words）

**应用场景**：用于扩展搜索范围，将用户query改写为语义相近但非完全等同的表达

**数据来源**：存储在waimai_d_search_query_rewrite(RESET)表和queryResetDBDict词表中

**典型特征：**

- 上下位词关系（如"水果"与"苹果"）
- 功效用途关系（如"烧水"与"水壶"）
- 组合品关系（如"洗发水沐浴露"与"浴液"）

**处理策略：**

- 上位词改写下位词：添加为同义词
- 下位词改写上位词：添加为改写词
- 功效用途改写：适合作为改写词而非同义词（如"定妆"与"粉扑"）

#### 3. 同义词（Synonyms）

**应用场景**：用于处理完全等价或高度近义的词对，确保不同表达能返回相同结果

**数据来源：**

- 自有词表：waimai_d_search_query_rewrite(SYNONYMS)表和querySynonymDBDict词表
- 美团平台接口：通过thrift服务异步获取

**典型特征：**

- 本质相同的表达（如"西红柿"与"番茄"）
- 品牌中英文对照（如"Nike"与"耐克"）
- 简写全称关系（如"北大"与"北京大学"）

**处理方式**：在实体识别后，对food/poi/category实体进行同义词替换（修饰+实体=>修饰+同义词）

#### 4. 核心区别对比

| 维度 | 纠错词 | 改写词 | 同义词 |
|------|-------|-------|-------|
| 关系强度 | 错误与正确关系 | 语义相关但不等同 | 完全或高度等价 |
| 召回策略 | 直接替换 | 多路召回，区分优先级 | 一次召回，走分词和标签索引 |
| 典型示例 | "麦党劳"→"麦当劳" | "水果"→"苹果" | "番茄"→"西红柿" |
| 数据更新 | 3000ms增量更新 | 3000ms增量更新 | 3000ms增量更新 |
| 影响范围 | 仅修正错误 | 扩展搜索范围 | 确保等价表达一致性 |

#### 5. 处理流程差异

**纠错词**：在归一化阶段直接应用，属于强制性修正

**同义词：**

- 自有词表：在一次召回阶段应用
- 平台同义词：在二次召回阶段应用

**改写词**：根据相关性分级处理，高相关(3级)视为同义词，低相关(1-2级)保持为改写词

