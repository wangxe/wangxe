go

### GMP与调度器
#### 线程模型（1:1；1:N；M:N）
#### GMP定义（为什么需要P）
#### 调度器初始化过程
m0 初始化 → g0 初始化 → schedt 初始化 → P 初始化 → sysmon（监控线程）创建 → 启动 main goroutine → 进入 main.main
#### 调度循环

### 内存分配
#### 栈内存与堆内存 （为什么go语言基本没有栈溢出）
#### 内存逃逸(逃逸条件)
#### 堆内存分配


### GC
#### 基础术语
* 根对象（Root）：GC 的起始扫描点，包括 Goroutine 栈上的指针、全局变量、寄存器中的指针等；
* 可达对象（Reachable）：从根对象出发，能通过指针引用链找到的对象（需要保留）；
* 不可达对象（Unreachable）：无法从根对象访问到的对象（可回收）；
* STW（Stop The World）：GC 过程中暂停所有业务 Goroutine 的阶段（Go 持续优化，STW 时间越来越短）。
#### 触发条件
* 堆内存占用达到「上一次 GC 后堆大小 × 内存增长率阈值」（默认 1.5 倍，可通过 GOGC 调整，GOGC=100 对应 2 倍）；
* 手动调用 runtime.GC()；
* 程序启动后超过 2 分钟未触发 GC（兜底）。
#### 主要流程 （初始标记->并发标记->并发清理->终止标记->并发清理收尾）
1. 触发：用户 M 分配内存 发现堆够大了 → 启动 GC
2. 初始标记：STW → 扫根 → 开写屏障 → 启动 GC Worker
3. 并发标记：GC Worker + 所有 M 一起标记，写屏障保护
4. 终止标记：STW → 补标记 → 关写屏障
5. 并发清理：分配内存时顺便回收垃圾

#### 写屏障
并发标记阶段，业务 Goroutine 可能修改指针（比如将一个黑色对象的指针指向白色对象），导致漏标。写屏障是「拦截指针修改的钩子函数」，核心规则：当黑色对象引用白色对象时，自动将白色对象标记为灰色，确保不会被误回收；

#### TIPS
* 为什么初始标记标记阶段和终止标记阶段需要stw，而其他阶段不需要（开启/关闭写屏障需要保证原子性）


### defer
执行顺序：函数逻辑 → return → 执行所有 defer → 函数退出
延迟执行：defer 后面的函数，不会立刻执行，而是等到当前函数即将返回时才执行
后进先出（LIFO）：多个 defer，最后定义的最先执行
参数预计算：defer 函数的参数在定义时就计算完毕，不是等到执行时才算
可以修改命名返回值：如果函数是命名返回值，defer 可以修改最终返回结果
即使函数崩溃（panic），defer 依然执行

### channel
channel 发送数据（ch <- x）底层做了什么？发送流程（runtime.chansend）
* 加锁（整个 channel 操作原子性）
* 如果 channel 已关闭 → panic
* 如果 有等待接收的 G（recvq 不为空）
    * 直接把数据拷贝给第一个等待的 G
    * 唤醒该 G
    * 无缓冲 channel 走这条路径
* 如果 缓冲区还有空闲
    * 数据写入环形队列
    * sendx++
    * 直接返回，不阻塞
* 否则（缓冲区满 / 无缓冲且无接收者）
    * 将当前 G 加入 sendq
    * 挂起当前 G（Gwaiting）
    * 释放 M，调度器去跑其他 G

channel 接收数据（<-ch）底层做了什么？接收流程（runtime.chanrecv）
* 加锁
* 如果 channel 未关闭 + 队列为空
    * 当前 G 加入 recvq
    * 挂起 G
* 如果 channel 已关闭 + 无数据
    * 返回 值类型零值 + false
* 如果 有等待发送的 G（sendq 不为空）
    * 从等待发送的 G 中直接取数据
    * 唤醒发送 G
* 如果 缓冲区有数据
    * 从环形队列取数据
    * recvx++
* 解锁返回

### map

### sync.Map
工作流程（超简单）
* 读优先查 read（无锁） → 命中直接返回，极快
* 没命中才加锁查 dirty
* misses 到一定阈值 → dirty 升级为 read
* 新写入 → 先进 dirty

为什么快？
* 读几乎不加锁
* 写只锁 dirty，不影响读
* 高并发读场景几乎无锁竞争