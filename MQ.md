MQ
基础
### 为什么要使用消息队列呢? （解耦，异步，削峰填谷，消息分发）

### 为什么要选择RocketMQ? （企业特性：事务消息、延时消息、死信队列、消息回溯、顺序消息）

### kafka 月rocketMQ选型
* 存储模型：Kafka 「顺序写 + 分区日志」的极致高效
    * Kafka：单分区严格顺序写磁盘+ 日志段文件
    * RocketMQ：混合存储 + 随机写的妥协
* 网络传输：Kafka 「批量传输 + 零拷贝 + 端到端压缩」的极致节流
* 架构设计：Kafka 「无状态 Broker + 分区并行」的极致扩展
* 底层实现：Kafka 「极简设计 + 批量处理」，RocketMQ 「特性丰富 + 单条处理」

| 指标 | Kafka（2.8+） | RocketMQ（4.9+） | 差异原因 |
|------|--------------|-----------------|---------|
| 生产 TPS | 100 万 +（批量 1000） | 20 万 +（批量 1000） | 顺序写 + 零拷贝 + 批量优化 |
| 消费 TPS | 80 万 +（批量 1000） | 15 万 +（批量 1000） | 分区并行 + 零拷贝 + 无状态 Broker |
| 磁盘 IO 利用率 | 90%+（顺序写） | 60%+（顺序 + 随机写） | ConsumeQueue 索引的随机 IO 开销 |
| 网络利用率 | 95%+（批量传输） | 70%+（批量传输） | 端到端压缩 + 零拷贝的优化 |
| GC 停顿时间 | 毫秒级（低频率） | 百毫秒级（高频率） | 批量处理 + JVM 深度优化 |

### RocketMQ消费模式?

| 维度 | Pull 模型 | Push 模型 | Pop 模型 |
|------|---------|---------|---------|
| 核心逻辑 | 消费者主动拉取 | 客户端封装 Pull，伪推送 | Broker 托管分配，消费者领消息 |
| 重平衡位置 | 客户端（需手动处理） | 客户端（自动但耗时长） | 服务端（Broker） |
| 重平衡耗时 | 无（需手动分配分区） | 秒级 / 分钟级（高并发下） | 几乎为 0（Broker 实时调整） |
| 消费进度存储 | 消费者本地 / 手动上报 | 客户端上报到 Broker | Broker 全托管 |
| 消费者状态 | 有状态（需记录分区） | 有状态（需协商分区） | 无状态（即插即用） |
| 消息重复风险 | 低（手动控制 Offset） | 中（重平衡可能重复分配） | 低（Broker 标记已领取） |
| 适用场景 | 定制化消费、低并发 | 中小并发、易用性优先 | 高并发（上千 / 万消费者）、低延迟 |
| 学习 / 开发成本 | 高（需手动处理拉取逻辑） | 低（只需写消费回调） | 中（新 API，逻辑简单） |


### RocketMQ核心组件

| 组件  | 核心定位  | 核心职责   | 部署特性  |
|---------------------|-----------------------|----------------|--------------------|
| Producer            | 消息生产者            | 1. 向 NameServer 获取 Broker 路由信息；<br>2. 按负载均衡规则向 Broker 发送消息；<br>3. 支持同步 / 异步 / 单向发送、批量发送、事务消息。                                               | 无状态，可无限水平扩容                                                                         |
| NameServer          | 轻量级路由注册中心    | 1. 管理 Broker 节点的注册与发现（Broker 定时心跳上报状态）；<br>2. 存储 Topic 与 Broker 的映射关系；<br>3. 为生产者 / 消费者提供路由查询服务。                                         | 无状态，集群部署（无主从），节点间不通信，靠 Broker 心跳保证数据一致                           |
| Broker              | 核心消息存储 / 转发节点 | 1. 接收生产者消息并持久化到磁盘（CommitLog+ConsumeQueue）；<br>2. 为消费者提供消息拉取 / 推送服务；<br>3. 管理消息生命周期（过期删除、重试、死信）；<br>4. 处理事务消息的二次确认、延时消息调度。 | 有状态，主从部署（Master 写 + Slave 读），支持多副本，可按 Topic 分片扩容                     |
| Consumer            | 消息消费者            | 1. 向 NameServer 获取 Broker 路由信息；<br>2. 按负载均衡 / 广播规则从 Broker 拉取（主流）/ 接收推送消息；<br>3. 消费消息并提交消费偏移量（Offset）；<br>4. 支持重试、死信队列、批量消费。 | 无状态，分集群消费/广播消费，可弹性扩容                                                       |
| Console/Dashboard   | 运维监控控制台        | 1. 可视化管理 Topic、Broker、消费者组；<br>2. 监控消息生产 / 消费 TPS、堆积、延迟；<br>3. 手动重试死信消息、回溯消费、管理配置。                                                        | 辅助组件，单 / 集群部署均可                                                                    |


进阶

### 如何保证消息的可用性/可靠性/不丢失呢？

* 在生产阶段，主要通过请求确认机制，来保证消息的可靠传递。
    1. 同步发送的时候，要注意处理响应结果和异常。如果返回响应 OK，表示消息成功发送到了 Broker，如果响应失败，或者发生其它异常，都应该重试。
    2. 异步发送的时候，应该在回调方法里检查，如果发送失败或者异常，都应该进行重试。
    3. 如果发生超时的情况，也可以通过查询日志的 API，来检查是否在 Broker 存储成功。

* 存储阶段，可以通过配置可靠性优先的 Broker 参数来避免因为宕机丢消息，简单说就是可靠性优先的场景都应该使用同步。
    1. 消息只要持久化到 CommitLog（日志文件）中，即使 Broker 宕机，未消费的消息也能重新恢复再消费。
    2. Broker 的刷盘机制：同步刷盘和异步刷盘，不管哪种刷盘都可以保证消息一定存储在 pagecache 中（内存中），但是同步刷盘更可靠，它是 Producer 发送消息后等数据持久化到磁盘之后再返回响应给 Producer。
    3. Broker 通过主从模式来保证高可用，Broker 支持 Master 和 Slave 同步复制、Master 和 Slave 异步复制模式，生产者的消息都是发送给 Master，但是消费既可以从 Master 消费，也可以从 Slave 消费。同步复制模式可以保证即使 Master 宕机，消息肯定在 Slave 中有备份，保证了消息不会丢失。
* 消费阶段
    * Consumer 保证消息成功消费的关键在于确认的时机，不要在收到消息后就立即发送消费确认，而是应该在执行完所有消费业务逻辑之后，再发送消费确认。因为消息队列维护了消费的位置，逻辑执行失败了，没有确认，再去队列拉取消息，就还是之前的一条。
    * 这样的话可能会出现业务逻辑耗时太长导致消息积压的问题，主要解决方案参考问题8
       



### 消息重复原因以及如何处理消息重复的问题呢?

| 方案   | 核心原理    | 适用场景      | 优点     | 缺点       |
|-----------------|----------------|----------------|---------------------|--------------------|
| 基于唯一 ID+Redis      | 用消息唯一 ID（如 msgId）或业务 ID 作为 Redis Key，消费前检查 Key 是否存在：<br>1. 不存在：执行逻辑，写入 Redis（设置过期时间）；<br>2. 存在：直接返回成功。 | 非核心业务、高频低价值消息（如日志、通知） | 实现简单、性能高（Redis 读写毫秒级）   | 若 Redis 宕机，可能丢失去重状态；需设置合理过期时间           |
| 基于数据库唯一索引     | 以业务唯一 ID（如订单 ID）作为数据库表的唯一索引，消费时执行 INSERT：<br>1. 插入成功：业务逻辑执行完成；<br>2. 唯一索引冲突：说明已处理，直接返回成功。 | 核心业务、数据持久化场景（如订单创建、支付回调） | 可靠性最高（数据库事务保障），无需额外存储 | 需提前设计唯一索引；插入失败需区分「唯一索引冲突」和其他异常 |
| 基于数据库状态机       | 业务表设计「状态字段」（如订单状态：待支付→已支付→已完成），消费时先检查状态：<br>1. 状态未处理：执行逻辑并更新状态；<br>2. 状态已处理：直接返回成功。 | 有状态流转的业务（如订单、物流）           | 贴合业务逻辑，无需额外去重存储         | 需保证状态更新的原子性（如加行锁）                           |
| 基于分布式锁           | 消费前用业务 ID 获取分布式锁（如 Redisson 锁）：<br>1. 获取到锁：执行逻辑；<br>2. 未获取到锁：说明已有线程处理，直接返回成功。                       | 高并发、需严格顺序处理的场景               | 避免并发下重复处理                     | 锁超时时间需合理配置；有锁竞争开销                            |

### 怎么处理消息积压?
* 提升消费并行度（最直接、成本最低）：提升消费者数量，提升queue的数量，调大消费线程数
* 消费逻辑异步化（剥离长耗时操作）：核心逻辑（必须成功的，如订单状态更新、数据入库）：在消费主线程执行，执行完立即发送确认；
非核心长耗时逻辑（如日志推送、短信发送、数据同步到数仓）：提交到本地线程池 / 消息队列（如另一个 MQ Topic）异步执行，异步任务需记录「执行状态」（如写入本地数据库 / Redis），失败后通过定时任务重试；
* 设置合理的消费超时时间（避免重复消费）

### 顺序消息如何实现?
RocketMQ 中，Topic 下的每个 Queue（队列）只能被「同一个消费者实例的同一个消费线程」消费（集群消费模式下）—— 这是实现顺序消费的底层保障：

### 如何实现消息过滤?（tags，sql，filter_server）

### 延时消息了解吗?（临时存储+定时任务）

### 怎么实现分布式消息事务的？半消息？
RocketMQ 事务消息的核心是「半消息（Half Message）」—— 先发送一条对消费者不可见的消息，待本地事务执行完成后，再确认消息是否可见；若本地事务结果未知（如系统崩溃），Broker 会主动回查生产者，确认事务状态。

### 死信队列知道吗?

### 如何保证RocketMQ的高可用
RocketMQ 高可用的核心是「去中心化 + 数据多副本 + 故障自动转移」，避免单点故障，同时保证数据不丢失、服务不中断：
* 无单点：NameServer 集群、Broker 主从集群、生产者 / 消费者均为无中心设计，单个节点故障不影响整体；
* 数据持久化 + 多副本：消息落地磁盘（CommitLog），且主 Broker 数据同步到从 Broker，避免单节点数据丢失；
    * 同步刷盘& 异步刷盘
    * 主从同步复制/异步复制
    
* 故障自动感知：NameServer 实时检测 Broker 心跳，消费者 / 生产者感知 Broker 故障后自动切换；
* 失败重试：生产 / 消费请求失败后，自动重试其他可用节点，降低单次故障的影响

原理

### 说一下 RocketMQ的整体工作流程？

### 为什么RocketMQ不使用 Zookeeper 作为注册中心呢？
* 基于可用性的考虑，Zookeeper满足的是 CP，也就是说 Zookeeper 并不能保证服务的可用性，Zookeeper 在进行选举的时候，整个选举的时间太长，期间整个集群都处于不可用的状态，而这对于一个注册中心来说肯定是不能接受的，作为服务发现来说就应该是为可用性而设计。
* 基于性能的考虑，NameServer 本身的实现非常轻量，而且可以通过增加机器的方式水平扩展，增加集群的抗压能力，而 Zookeeper 的写是不可扩展的，Zookeeper 要解决这个问题只能通过划分领域，划分多个 Zookeeper 集群来解决，首先操作起来太复杂，其次这样还是又违反了 CAP 中的 A 的设计，导致服务之间是不连通的。
* 持久化的机制来带的问题，ZooKeeper 的 ZAB 协议对每一个写请求，会在每个 ZooKeeper 节点上保持写一个事务日志，同时再加上定期的将内存数据镜像（Snapshot）到磁盘来保证数据的一致性和持久性，而对于一个简单的服务发现的场景来说，这其实没有太大的必要，这个实现方案太重了。而且本身存储的数据应该是高度定制化的。
* 消息发送应该弱依赖注册中心，而 RocketMQ 的设计理念也正是基于此，生产者在第一次发送消息的时候从 NameServer 获取到 Broker 地址后缓存到本地，如果 NameServer 整个集群不可用，短时间内对于生产者和消费者并不会产生太大影响。


### Broker是怎么保存数据的
    CommitLog 文件、ConsumeQueue 文件、Indexfile 文件。

### 说说RocketMQ怎么对文件进行读写的
pagecache

### 消息刷盘怎么实现的呢?

### 能说下 RocketMQ的负载均衡是如何实现的

### RocketMQ消息长轮询了解吗